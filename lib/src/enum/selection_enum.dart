/// Defines the filtering options for the media picker.
enum SelectionEnum {
  /// Allows selecting both images and videos.
  MultiMedia,
  
  /// Filters for images only.
  Images,
  
  /// Filters for videos only.
  Videos
}